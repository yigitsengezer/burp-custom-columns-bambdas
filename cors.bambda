if (!requestResponse.hasResponse()) {
    return "";
}
if (requestResponse.response().hasHeader("Access-Control-Allow-Credentials") &&
    requestResponse.response().headerValue("Access-Control-Allow-Credentials").equalsIgnoreCase("true")) {
    return "CORS Vulnerable";
}
return "";
